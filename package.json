{
    "name": "graphscript-monorepo",
    "workspaces": [
        "**"
    ],
    "scripts": {
        "start": "cd js && tinybuild && cd packages/backend && tinybuild && cd ../core && tinybuild && cd ../../extras && npm start"
    }
}
