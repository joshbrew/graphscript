{
    "name": "graphscript-monorepo",
    "workspaces": [
        "**"
    ],
    "scripts": {
        "start": "cd packages/graphscript && tinybuild && cd ../graphscript-node && tinybuild && cd ../graphscript-core && tinybuild && cd ../../extras && npm start"
    }
}
