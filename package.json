{
    "name": "graphscript-monorepo",
    "version": "0.2.28",
    "description": "Comprehensive acyclic-graph based application architecture with microservices and networking",
    "main": "dist/index.js",
    "module": "dist/index.esm.js",
    "types": "dist/index.d.ts",
    "type": "module",
    "private": true,
    "workspaces": [
        "**"
    ],
    "scripts": {
        "start": "npm run build",
        "build": "cd build && cd main && tinybuild && cd ../node && tinybuild path=tinybuild.node.config.js && cd ../core && tinybuild path=tinybuild.core.config.js && cd ../../src/extras && npm run build",
        "serve": "tinybuild serve",
        "init": "node tinybuild/init.js",
        "concurrent": "concurrently \"npm run python\" \"npm start\"",
        "dev": "npm run pip && npm i --save-dev concurrently && npm i --save-dev nodemon && npm run concurrent",
        "startdev": "nodemon --exec \"node tinybuild.js\" -e ejs,js,ts,jsx,tsx,css,html,jpg,png,scss,txt,csv",
        "python": "python python/server.py",
        "pip": "pip install quart && pip install websockets",
        "pwa": "npm i workbox-cli && workbox generateSW node_server/pwa/workbox-config.js && npm run build && npm start",
        "test": "NODE_OPTIONS=--experimental-vm-modules jest --runInBand --coverage",
        "bench": "cd benchmarks && ts-node index.ts"
    },
    "keywords": [
        "esbuild"
    ],
    "author": "Joshua Brewster",
    "license": "AGPL-3.0-or-later",
    "devDependencies": {
        "@jest/globals": "^29.3.1",
        "@types/jest": "^29.2.4",
        "@types/node": "~18.7.15",
        "@types/ws": "~8.5.3",
        "jest": "^29.3.1",
        "ts-jest": "^29.0.3",
        "ts-node": "^10.9.1",
        "typescript": "^4.9.4"
    }
}
