{
    "name": "graphscript-extras",
    "version": "0.2.27",
    "description": "Extra services for graphscript.",
    "private": true,
    "scripts": {
        "start": "npm run build",
        "build": "cd build && cd gpu && tinybuild && cd ../storage && tinybuild && cd ../services && tinybuild"
    },
    "keywords": [
        "esbuild"
    ],
    "author": "Joshua Brewster",
    "license": "AGPL-3.0-or-later"
}
