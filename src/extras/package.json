{
    "name": "graphscript-extras",
    "version": "0.2.27",
    "description": "Extra services for graphscript.",
    "private": true,
    "scripts": {
        "start": "npm run build",
        "build": "cd build && cd gpu && tinybuild path=tinybuild.gpu.config.js && cd ../storage && tinybuild path=tinybuild.storage.config.js && cd ../services && tinybuild path=tinybuild.services.config.js"
    },
    "keywords": [
        "esbuild"
    ],
    "author": "Joshua Brewster",
    "license": "AGPL-3.0-or-later"
}
